

const Button = ( {styles} ) => {
  return (
    <button type="button" className={`font-poppins rounded-[10px] text-primary text-[18px] font-medium py-4 px-6 bg-blue-gradient outline-none ${styles}`}>
      Get Started
    </button>
  )
}

export default Button